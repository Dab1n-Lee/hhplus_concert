spring:
  config:
    activate:
      on-profile: test
  jpa:
    open-in-view: false
    show-sql: false
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate.jdbc.time_zone: UTC
  sql:
    init:
      mode: never
